"use client";
import { FormEvent, useState } from "react";
import { useSession } from "next-auth/react";
import { useRouter } from "next/navigation";
export default function SetupAccount(){const {status}=useSession();const router=useRouter();const [location,setLocation]=useState("");const [targetRole,setTargetRole]=useState("");const [experienceYears,setExperienceYears]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);if(status==="loading")return <main className="auth-shell"><section className="auth-card"><p>Loading…</p></section></main>;if(status!=="authenticated")return <main className="auth-shell"><section className="auth-card"><h1>Please log in</h1><p className="sub">Your email is verified. Log in to finish setting up your account.</p></section></main>;async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);const r=await fetch("/api/account/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({location,targetRole,experienceYears})});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||"Could not save your profile.");return}router.push("/dashboard");}return <main className="auth-shell"><section className="auth-card"><div className="brand">JobFit<span>Pro</span></div><h1>Set up your account</h1><p className="sub">Tell us a little about the jobs you want to find.</p><form onSubmit={submit}><label>Current location<input value={location} onChange={e=>setLocation(e.target.value)} placeholder="Delhi NCR" required /></label><label>Target job role<input value={targetRole} onChange={e=>setTargetRole(e.target.value)} placeholder="Project Manager" required /></label><label>Years of experience<input type="number" min="0" max="60" value={experienceYears} onChange={e=>setExperienceYears(e.target.value)} placeholder="8" required /></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={loading}>{loading?"Saving…":"Finish setup"}</button></form></section></main>}
